# Lesson cards Landing Page

This project was developed with HTML5, Sass, React, Redux, Axios, Redux-thunk.

## Quick start:

* clone the repo
* Download dependencies `npm install`
* Runs the app in the development mode `npm start`